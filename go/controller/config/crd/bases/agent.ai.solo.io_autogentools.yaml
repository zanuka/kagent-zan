---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: autogentools.agent.ai.solo.io
spec:
  group: agent.ai.solo.io
  names:
    kind: AutogenTool
    listKind: AutogenToolList
    plural: autogentools
    singular: autogentool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AutogenTool is the Schema for the autogentools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AutogenToolSpec defines the desired state of AutogenTool.
            properties:
              description:
                type: string
            type: object
          status:
            description: AutogenToolStatus defines the observed state of AutogenTool.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
