---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: autogenagents.agent.ai.solo.io
spec:
  group: agent.ai.solo.io
  names:
    kind: AutogenAgent
    listKind: AutogenAgentList
    plural: autogenagents
    singular: autogenagent
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AutogenAgent is the Schema for the autogenagents API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AutogenAgentSpec defines the desired state of AutogenAgent.
            properties:
              description:
                type: string
              name:
                type: string
              systemMessage:
                type: string
              tools:
                items:
                  properties:
                    config:
                      additionalProperties:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      description: 'note: this implementation is due to the kubebuilder
                        limitation https://github.com/kubernetes-sigs/controller-tools/issues/636'
                      type: object
                    provider:
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: AutogenAgentStatus defines the observed state of AutogenAgent.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
